{
  "name": "cursnode",
  "version": "1.0.0",
  "description": "Curs de Node Cibernarium",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index",
    "deploy": "npm run test && npm run build && npm run deploy"
  },
  "environment": {
    "node": "14.17.3"
  },
  "author": "Pedro Nieto",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.12"
  }
}
